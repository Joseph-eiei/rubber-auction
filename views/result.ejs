<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>ผลการประมูลวันที่ <%= displaydate %></title>
</head>
<body>
  <h1>ผลการประมูลยางพารา วันที่ <%= displaydate %></h1>
  <table border="1" cellpadding="6" cellspacing="0">
    <tr><th>ชื่อ</th><th>ราคา (บาท)</th></tr>
    <% data.forEach(item => { %>
      <tr>
        <td><%= item.name %></td>
        <td><%= item.price %></td>
      </tr>
    <% }) %>
  </table>
</body>
</html>
